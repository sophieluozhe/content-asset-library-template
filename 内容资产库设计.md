---
类型: 内容资产库设计
状态: 已确认
版本: 2
创建日期: 2026-09-20
---

# 内容资产库设计

## 目标

将灵感、录音转写、书籍/课程笔记、案例与内容拆解加工成可追溯、可复用的原子资产；以同一主题派生短视频、公众号和朋友圈内容，并管理看板、排期与发布复盘。

## 总体流程

原始材料 → 清洗稿 → 原子资产 → 主题 → 内容单 → 发布 → 复盘 → 新资产。

原始材料永远保留，不覆盖、不删除；清洗稿和每张卡都必须链接来源，并标注页码、时间戳或段落锚点。

## 目录结构

```text
内容资产库/
├── 00 内容控制台/
│   ├── 资产加工看板.md
│   ├── 内容生产看板.md
│   ├── 发布日历.md
│   └── 发布复盘.md
├── 01 输入资产/
│   ├── 01 原始材料/
│   ├── 02 清洗工作台/
│   ├── 03 清洗完成/
│   ├── 04 萃取工作台/
│   └── 05 已萃取来源/
├── 02 原子资产/
│   ├── 观点卡/
│   ├── 方法卡/
│   ├── 案例卡/
│   ├── 反例卡/
│   ├── 边界卡/
│   ├── 定义卡/
│   ├── 证据卡/
│   └── 故事卡/
├── 03 主题库/
├── 04 内容单/
│   ├── 短视频/
│   ├── 公众号/
│   └── 朋友圈/
├── 05 已发布归档/
└── 99 模板/
```

## 看板状态

### 资产加工看板

待清洗 → 清洗中 → 待萃取 → 萃取中 → 已完成 → 暂缓。

处理中由清洗稿和萃取工作单的属性表示，不移动或覆盖原始材料。

### 内容生产看板

待策划 → 创作中 → 待审核 → 待发布 → 已发布 → 暂缓。

发布日历只展示有“发布日期”的内容单。

## 原子资产

| 类型 | 定义 |
| --- | --- |
| 观点卡 | 可主张的判断 |
| 方法卡 | 可执行步骤或原则 |
| 案例卡 | 真实做法及结果 |
| 反例卡 | 失败或失效情境，关联被挑战的观点或方法 |
| 边界卡 | 适用前提和不适用情境，关联被限制的观点或方法 |
| 定义卡 | 关键概念的含义和区分 |
| 证据卡 | 数据、原话或研究，关联它支持的观点 |
| 故事卡 | 场景、冲突、转折 |

金句不单独成卡，记录在对应观点、故事或方法卡的“可直接引用表达”中。

## 最小字段与受控值

| 对象 | 字段 |
| --- | --- |
| 输入、清洗、萃取 | 类型、状态、来源类型、原始来源、处理稿、权限、录入日期 |
| 原子资产 | 类型、来源（列表）、原文锚点（列表）、权限、权限依据、置信度、置信度说明、使用状态、适用平台、关系 |
| 主题 | 类型、状态、核心问题、核心主张、关联资产 |
| 内容单 | 类型、平台、状态、主题、关联资产、权限、权限依据、版本、首次发布日期、最后修改日期、发布日期、发布链接 |
| 发布复盘 | 内容单、发布链接、版本、复盘日期、回流资产 |

- 权限只可填写：`可公开使用`、`需匿名`、`仅供学习`、`待确认授权`。派生资产和内容单不得比任一来源或关联资产更宽松；拿不准时一律为`仅供学习`或`待确认授权`。
- 置信度只可填写：`已核验`、`待核`、`推断`、`演示`。理由写在`置信度说明`，不要把说明混进受控值。
- 使用状态只可填写：`候选`、`可复用`、`已使用`、`已淘汰`。它只表示复用生命周期，不表示公开权限。
- `来源`和`原文锚点`均为列表；一张卡只服务一个主要主张，多个来源分别列出，避免把不同事实混成一条证据。

## 规则

1. 保留原件。
2. 保留锚点。
3. 关系只在“关系发起卡”记录一次：证据卡用`支持 → [[目标卡]]`，反例卡用`挑战 → [[目标卡]]`，边界卡用`限制 → [[目标卡]]`，案例卡用`佐证 → [[目标卡]]`；不要在被关联卡重复维护反向清单。
4. 原子资产的权限继承最严格来源；内容单的权限继承最严格关联资产。`使用状态: 可复用`不等于可公开。
5. 文件属性中的`状态`是唯一状态来源；加工和生产看板只聚合链接，不另行记录状态。
6. 区分可复用卡片与一次性成品。
7. 每次准备发布时递增内容单`版本`；首次发布写入`首次发布日期`，修改内容写入`最后修改日期`。发布复盘必须记录对应版本。
8. 发布后记录链接、反馈、有效表达和待验证问题，并回流为新资产。

## 第一版限制

- 仅 Markdown、链接和属性，不依赖第三方插件。
- 不自动抓取外部平台数据。
- 发布由用户执行。
- 一月实用后再考虑自动化和复杂评分。

## 验收标准

- 能放入原始转写并在加工看板看状态。
- 能生成清洗稿和带来源锚点的卡片。
- 同主题可关联短视频、公众号、朋友圈内容单。
- 生产看板可按状态查看，日历可按日期查看。
- 发布内容有链接和复盘，并回流资产。
